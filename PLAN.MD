take episodes.txt (line 1 is a header) and craft an efficient golang script that will run this command for each line pair. use concurrency where possible: 

java -jar abyss-dl.jar ${KEY} h -o ./tv/SSNHP/{$NUM}.mp4

